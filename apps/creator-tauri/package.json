{
  "name": "creator-tauri-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "lint": "npm run typecheck",
    "quality": "npm run typecheck && npm run build",
    "preview": "vite preview",
    "tauri:dev": "./scripts/prepare-linux-compat.sh && ./scripts/prepare-orchestrator-base.sh debug && PKG_CONFIG_PATH=\"$(cd ../../pkgconfig && pwd):$PKG_CONFIG_PATH\" RUSTFLAGS=\"-L native=$(cd ../../libshims && pwd) $RUSTFLAGS\" npm exec -- tauri dev --features tauri-commands",
    "tauri:build": "./scripts/prepare-linux-compat.sh && ./scripts/prepare-orchestrator-base.sh release && PKG_CONFIG_PATH=\"$(cd ../../pkgconfig && pwd):$PKG_CONFIG_PATH\" RUSTFLAGS=\"-L native=$(cd ../../libshims && pwd) $RUSTFLAGS\" npm exec -- tauri build -b none --features tauri-commands,custom-protocol",
    "tauri:bundle": "./scripts/prepare-linux-compat.sh && ./scripts/prepare-orchestrator-base.sh release && PKG_CONFIG_PATH=\"$(cd ../../pkgconfig && pwd):$PKG_CONFIG_PATH\" RUSTFLAGS=\"-L native=$(cd ../../libshims && pwd) $RUSTFLAGS\" npm exec -- tauri build --features tauri-commands,custom-protocol"
  },
  "dependencies": {
    "@kobalte/core": "^0.13.11",
    "@tabler/icons-solidjs": "^3.37.1",
    "@tauri-apps/api": "^1.6.0",
    "autoprefixer": "^10.4.24",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "postcss": "^8.5.6",
    "solid-js": "^1.9.0",
    "solid-sonner": "^0.2.8",
    "tailwind-merge": "^3.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.0",
    "@types/node": "^25.3.0",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vite-plugin-solid": "^2.10.0"
  }
}
